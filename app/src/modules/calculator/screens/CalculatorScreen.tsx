import { type ReactNode,useEffect,useMemo,useRef,useState } from 'react';
import Ionicons from '@expo/vector-icons/Ionicons';
import { PanResponder,Pressable,StyleSheet,useWindowDimensions,View } from 'react-native';
import { Text, TextInput } from '../../../i18n';
import { router } from 'expo-router';
import { api,money } from '../../../api';
import { AppShell } from '../../../components/AppShell';
import { theme } from '../../../theme';
import { decimalInput,integerInput } from '../../../inputFormatters';
import { projectDaysFromStages } from '../../../servicePlanning';

type Row={label:string;value:string};
type ServiceTeamMember={role:string;dailyRateCents:number;included?:boolean};
type ServiceCost={type:'variable'|'fixed'|string;description:string;amountCents:number};
type ServiceStage={duration?:string|null};
type ServiceOption={id:string;code?:string|null;name:string;active?:boolean;defaultDays:number;safetyMarginBps:number;dailyRateCents:number;variableCostCents?:number|null;fixedCostCents?:number|null;team?:ServiceTeamMember[];costs?:ServiceCost[];stages?:ServiceStage[]};
type PricingResult={laborCents:number;variableCents:number;fixedCents:number;marginCents:number;totalCents:number};
type PricingPayload={dailyRateCents:number;days:number;people:number;variableCostCents:number;fixedCostCents:number;safetyMarginBps:number};
const cents=(value:string)=>Math.max(0,Math.round((Number(value.replace(',','.'))||0)*100));
const reais=(value:number)=>String(value/100).replace('.',',');

export default function CalculatorScreen(){
  const[services,setServices]=useState<ServiceOption[]>([]); const[selectedIds,setSelectedIds]=useState<string[]>([]);
  const[days,setDays]=useState('1'); const[margin,setMargin]=useState('50'); const[minimumDaily,setMinimumDaily]=useState(0);
  const[team,setTeam]=useState<Row[]>([]); const[variable,setVariable]=useState<Row[]>([]); const[fixed,setFixed]=useState<Row[]>([]); const[result,setResult]=useState<PricingResult>();
  const {width}=useWindowDimensions(); const compact=width<1040; const wide=width>=1440;
  useEffect(()=>{api<ServiceOption[]>('/services').then(rows=>setServices(rows.filter(x=>x.active!==false)))},[]);
  const selectedServices=useMemo(()=>selectedIds.map(id=>services.find(s=>s.id===id)).filter((s):s is ServiceOption=>!!s),[selectedIds,services]);
  useEffect(()=>{
    if(!selectedServices.length){setTeam([]);setVariable([]);setFixed([]);setMinimumDaily(0);setDays('1');return}
    const projectDays=selectedServices.reduce((sum,s)=>sum+projectDaysFromStages(s.stages,s.defaultDays),0);
    setDays(String(Math.max(1,projectDays)));
    setMargin(String(Math.max(...selectedServices.map(s=>s.safetyMarginBps??0))/100));
    setMinimumDaily(Math.max(...selectedServices.map(s=>s.dailyRateCents||0)));
    const nextTeam:Row[]=[];const nextVariable:Row[]=[];const nextFixed:Row[]=[];
    selectedServices.forEach(service=>{
      const prefix=selectedServices.length>1?`${service.code||service.name} · `:'';
      const members=service.team?.filter(x=>x.included!==false)??[];
      members.forEach(x=>nextTeam.push({label:`${prefix}${x.role}`,value:reais(x.dailyRateCents)}));
      const variableCosts=service.costs?.filter(x=>x.type==='variable')??[];
      const fixedCosts=service.costs?.filter(x=>x.type==='fixed')??[];
      if(variableCosts.length)variableCosts.forEach(x=>nextVariable.push({label:`${prefix}${x.description}`,value:reais(x.amountCents)}));
      else if(service.variableCostCents)nextVariable.push({label:`${prefix}Despesas diárias`,value:reais(service.variableCostCents)});
      if(fixedCosts.length)fixedCosts.forEach(x=>nextFixed.push({label:`${prefix}${x.description}`,value:reais(x.amountCents)}));
      else if(service.fixedCostCents)nextFixed.push({label:`${prefix}Custo do projeto`,value:reais(service.fixedCostCents)});
    });
    const hasResponsible=nextTeam.some(x=>/p\.?o\.?|respons[aá]vel|minha di[aá]ria/i.test(x.label));
    if(!hasResponsible)nextTeam.unshift({label:'P.O. responsável',value:'0'});
    setTeam(nextTeam);setVariable(nextVariable);setFixed(nextFixed);
  },[selectedServices]);
  const teamDaily=team.reduce((n,x)=>n+cents(x.value),0);
  const effectiveDaily=Math.max(teamDaily,minimumDaily);
  const payload=useMemo<PricingPayload>(()=>({dailyRateCents:effectiveDaily,days:Math.max(1,Number(days)||1),people:1,variableCostCents:variable.reduce((n,x)=>n+cents(x.value),0),fixedCostCents:fixed.reduce((n,x)=>n+cents(x.value),0),safetyMarginBps:Math.max(0,Math.round((Number(margin.replace(',','.'))||0)*100))}),[effectiveDaily,days,variable,fixed,margin]);
  useEffect(()=>{if(!payload.dailyRateCents){setResult(undefined);return} const timer=setTimeout(()=>api<PricingResult>('/pricing/calculate',{method:'POST',body:JSON.stringify(payload)}).then(setResult),220);return()=>clearTimeout(timer)},[payload]);
  const toggle=(id:string)=>setSelectedIds(ids=>ids.includes(id)?ids.filter(x=>x!==id):[...ids,id]);
  const move=(index:number,direction:-1|1)=>setSelectedIds(ids=>{const target=index+direction;if(target<0||target>=ids.length)return ids;const copy=[...ids];[copy[index],copy[target]]=[copy[target],copy[index]];return copy});
  return <AppShell title="Calculadora Rápida" subtitle="Monte o projeto com um ou mais serviços e simule o preço em tempo real.">
    <View style={[s.layout,wide&&s.layoutWide,compact&&s.layoutCompact]}><View style={s.formColumn}>
      <Card title="Serviços do projeto" hint="selecione um ou mais"><View style={s.services}>{services.map(x=>{const on=selectedIds.includes(x.id);return <Pressable key={x.id} onPress={()=>toggle(x.id)} style={[s.service,on&&s.serviceOn]}><Ionicons name={on?'checkmark-circle':'add-circle-outline'} size={18} color={on?theme.green2:theme.muted}/><Text style={[s.serviceText,on&&s.serviceTextOn]}>{x.code?`${x.code} — `:''}{x.name}</Text><Text style={s.serviceDays}>{projectDaysFromStages(x.stages,x.defaultDays)}d</Text></Pressable>})}</View>{selectedServices.length>1&&<View style={s.selectedList}><Text style={s.fieldLabel}>Ordem dos serviços — arraste pelo ícone</Text>{selectedServices.map((service,index)=><DraggableService key={service.id} name={`${service.code?service.code+' — ':''}${service.name}`} index={index} total={selectedServices.length} move={move}/>)}</View>}</Card>
      <Card title="Parâmetros"><View style={s.twoFields}><Field label="Quantidade de dias (pelas etapas)" value={days} onChange={(v:string)=>setDays(integerInput(v,3))}/><Field label="Margem de segurança (%)" value={margin} onChange={(v:string)=>setMargin(decimalInput(v,2,999.99))}/></View>{minimumDaily>0&&<Text style={s.minimum}>Diária mínima cadastrada: {money(minimumDaily)}. Se a equipe ficar abaixo desse valor, o mínimo é aplicado automaticamente.</Text>}</Card>
      <EditableCard title="Equipe" hint="diária por profissional" rows={team} setRows={setTeam} addLabel="Assistente"/>
      <EditableCard title="Custos Variáveis" hint="por dia" rows={variable} setRows={setVariable} addLabel="Novo custo"/>
      <EditableCard title="Custos Fixos" hint="por projeto" rows={fixed} setRows={setFixed} addLabel="Novo custo"/>
    </View>
    <View style={[s.resultColumn,compact&&s.resultColumnCompact]}><View style={s.resultCard}><View style={s.resultHead}><Text style={s.resultTitle}>Resultado</Text><Ionicons name="sparkles-outline" size={18} color={theme.gold}/></View>{result?<View><ResultLine label="Diária aplicada" value={payload.dailyRateCents}/>{minimumDaily>teamDaily&&<ResultLine label="Equipe informada" value={teamDaily}/>}<ResultLine label={`Total equipe × ${payload.days}d`} value={result.laborCents}/><ResultLine label={`Custo variável × ${payload.days}d`} value={result.variableCents}/><ResultLine label="Custo fixo" value={result.fixedCents}/><ResultLine label={`Margem (${margin||0}%) sobre diária da equipe + variáveis`} value={result.marginCents}/><View style={s.total}><Text style={s.totalLabel}>Total</Text><Text style={s.totalValue}>{money(result.totalCents)}</Text></View></View>:<View style={s.placeholder}><Ionicons name="calculator-outline" size={30} color="rgba(255,255,255,.25)"/><Text style={s.placeholderText}>Selecione um serviço ou adicione a equipe para ver o cálculo</Text></View>}</View>
      {result&&<Pressable style={s.save} onPress={()=>router.push('/quotes?new=1')}><Ionicons name="document-text-outline" size={17} color={theme.g900}/><Text style={s.saveText}>Salvar como Orçamento</Text></Pressable>}
      <View style={s.tip}><Ionicons name="information-circle-outline" size={18} color={theme.green2}/><Text style={s.tipText}>Custos variáveis são multiplicados pelos dias. Custos fixos entram uma única vez. A margem de segurança considera a diária da equipe + o total dos custos variáveis.</Text></View>
    </View></View>
  </AppShell>;
}

function DraggableService({name,index,total,move}:{name:string;index:number;total:number;move:(index:number,direction:-1|1)=>void}){const start=useRef(0);const pan=useRef(PanResponder.create({onStartShouldSetPanResponder:()=>true,onMoveShouldSetPanResponder:(_,g)=>Math.abs(g.dy)>6,onPanResponderGrant:()=>{start.current=Date.now()},onPanResponderRelease:(_,g)=>{if(Math.abs(g.dy)>24)move(index,g.dy<0?-1:1)}})).current;return <View style={s.dragCard}><View {...pan.panHandlers} style={s.dragHandle}><Ionicons name="reorder-three-outline" size={22} color={theme.muted}/></View><Text style={s.dragText}>{name}</Text><Pressable disabled={index===0} onPress={()=>move(index,-1)} style={s.smallMove}><Ionicons name="chevron-up" size={16} color={index===0?theme.borderStrong:theme.green2}/></Pressable><Pressable disabled={index===total-1} onPress={()=>move(index,1)} style={s.smallMove}><Ionicons name="chevron-down" size={16} color={index===total-1?theme.borderStrong:theme.green2}/></Pressable></View>}
function Card({title,hint,children}:{title:string;hint?:string;children:ReactNode}){return <View style={s.card}><View style={s.cardTitleRow}><Text style={s.cardTitle}>{title}</Text>{hint&&<Text style={s.cardHint}>({hint})</Text>}</View>{children}</View>}
function EditableCard({title,hint,rows,setRows,addLabel}:{title:string;hint:string;rows:Row[];setRows:(x:Row[])=>void;addLabel:string}){const update=(i:number,key:keyof Row,value:string)=>setRows(rows.map((x,j)=>j===i?{...x,[key]:value}:x));return <View style={s.card}><View style={s.editHead}><View style={s.cardTitleRow}><Text style={s.cardTitle}>{title}</Text><Text style={s.cardHint}>({hint})</Text></View><Pressable style={s.add} onPress={()=>setRows([...rows,{label:addLabel,value:'0'}])}><Ionicons name="add" size={15} color={theme.green2}/><Text style={s.addText}>Adicionar</Text></Pressable></View>{rows.length===0?<Text style={s.emptyRow}>Nenhum item adicionado.</Text>:rows.map((row,i)=><View key={`${title}-${i}`} style={s.editRow}><TextInput value={row.label} onChangeText={(v:string)=>update(i,'label',v)} placeholder="Descrição" style={[s.input,s.nameInput]}/><View style={s.moneyInput}><Text style={s.currency}>R$</Text><TextInput value={row.value} onChangeText={(v:string)=>update(i,'value',decimalInput(v))} keyboardType="decimal-pad" style={s.moneyField}/></View><Pressable style={s.trash} onPress={()=>setRows(rows.filter((_,j)=>j!==i))}><Ionicons name="trash-outline" size={17} color={theme.danger}/></Pressable></View>)}</View>}
function Field({label,value,onChange}:{label:string;value:string;onChange:(v:string)=>void}){return <View style={s.field}><Text style={s.fieldLabel}>{label}</Text><TextInput value={value} onChangeText={onChange} keyboardType="decimal-pad" style={s.input}/></View>}
function ResultLine({label,value}:{label:string;value:number}){return <View style={s.resultLine}><Text style={s.resultLabel}>{label}</Text><Text style={s.resultValue}>{money(value)}</Text></View>}
const s=StyleSheet.create({layout:{flexDirection:'row',alignItems:'flex-start',gap:24,width:'100%'},layoutWide:{gap:28},layoutCompact:{flexDirection:'column'},formColumn:{flex:1,gap:16,width:'100%',minWidth:0},resultColumn:{width:350,gap:14,flexShrink:0},resultColumnCompact:{width:'100%'},card:{backgroundColor:theme.white,borderWidth:1,borderColor:theme.border,borderRadius:16,padding:20,shadowColor:theme.shadow,shadowOpacity:.035,shadowRadius:12,shadowOffset:{width:0,height:4},elevation:1},cardTitleRow:{flexDirection:'row',alignItems:'center',gap:5},cardTitle:{fontSize:13,fontWeight:'800',color:theme.ink},cardHint:{fontSize:11,color:theme.muted},fieldLabel:{fontSize:12,fontWeight:'700',color:theme.muted,marginBottom:7},services:{gap:7,marginTop:14},service:{borderWidth:1,borderColor:theme.border,borderRadius:9,paddingHorizontal:12,paddingVertical:10,backgroundColor:theme.white,flexDirection:'row',alignItems:'center',gap:8},serviceOn:{borderColor:theme.green2,backgroundColor:theme.green50},serviceText:{fontSize:13,color:theme.muted,flex:1},serviceTextOn:{color:theme.green2,fontWeight:'700'},serviceDays:{fontSize:11,fontWeight:'800',color:theme.muted},selectedList:{marginTop:14,gap:6},dragCard:{minHeight:44,borderWidth:1,borderColor:theme.border,borderRadius:10,flexDirection:'row',alignItems:'center',paddingHorizontal:8,backgroundColor:'#FCFDFC'},dragHandle:{padding:8},dragText:{flex:1,fontSize:12,fontWeight:'700',color:theme.ink},smallMove:{padding:7},twoFields:{flexDirection:'row',gap:12,marginTop:14,flexWrap:'wrap'},field:{flex:1,minWidth:180},minimum:{fontSize:11,color:theme.green2,marginTop:10,lineHeight:16},input:{borderWidth:1,borderColor:theme.border,borderRadius:10,paddingHorizontal:11,paddingVertical:10,fontSize:13,color:theme.ink,backgroundColor:theme.white},editHead:{flexDirection:'row',alignItems:'center',justifyContent:'space-between'},add:{flexDirection:'row',alignItems:'center',gap:3,padding:6},addText:{fontSize:11,fontWeight:'800',color:theme.green2},emptyRow:{fontSize:11,color:theme.muted,marginTop:12},editRow:{flexDirection:'row',alignItems:'center',gap:8,marginTop:9},nameInput:{flex:1},moneyInput:{width:130,height:42,borderWidth:1,borderColor:theme.border,borderRadius:10,flexDirection:'row',alignItems:'center',paddingHorizontal:10},currency:{fontSize:12,color:theme.muted},moneyField:{flex:1,fontSize:13,color:theme.ink,paddingVertical:8},trash:{padding:9},resultCard:{backgroundColor:theme.green,borderRadius:16,padding:20},resultHead:{flexDirection:'row',alignItems:'center',justifyContent:'space-between',marginBottom:12},resultTitle:{fontFamily:'serif',fontSize:20,fontWeight:'700',color:theme.goldLight},resultLine:{flexDirection:'row',justifyContent:'space-between',gap:12,paddingVertical:9},resultLabel:{flex:1,fontSize:12,color:'rgba(255,255,255,.62)'},resultValue:{fontSize:13,fontWeight:'800',color:theme.white},total:{borderTopWidth:1,borderTopColor:'rgba(255,255,255,.16)',marginTop:6,paddingTop:16,flexDirection:'row',justifyContent:'space-between',alignItems:'center'},totalLabel:{fontFamily:'serif',fontSize:18,fontWeight:'700',color:theme.goldLight},totalValue:{fontFamily:'serif',fontSize:22,fontWeight:'700',color:theme.goldLight},placeholder:{alignItems:'center',paddingVertical:34,gap:9},placeholderText:{fontSize:12,color:'rgba(255,255,255,.5)',textAlign:'center'},save:{minHeight:48,borderRadius:11,backgroundColor:theme.gold,flexDirection:'row',alignItems:'center',justifyContent:'center',gap:8},saveText:{fontSize:12,fontWeight:'900',color:theme.g900},tip:{borderRadius:11,backgroundColor:theme.green50,padding:12,flexDirection:'row',alignItems:'flex-start',gap:8},tipText:{flex:1,fontSize:11,color:theme.green2,lineHeight:16}});
