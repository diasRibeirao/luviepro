import { Ionicons } from '@expo/vector-icons';
import { Pressable,StyleSheet,TextInput,View } from 'react-native';
import { theme } from '../theme';
export function SearchField({value,onChangeText,placeholder}:{value:string;onChangeText:(value:string)=>void;placeholder:string}){return <View style={s.search}><Ionicons name="search-outline" size={17} color={theme.muted}/><TextInput value={value} onChangeText={onChangeText} placeholder={placeholder} placeholderTextColor={theme.muted} autoCapitalize="none" autoCorrect={false} style={s.input}/>{value?<Pressable accessibilityLabel="Limpar busca" onPress={()=>onChangeText('')} style={s.clear}><Ionicons name="close-circle" size={18} color={theme.muted}/></Pressable>:null}</View>}
const s=StyleSheet.create({search:{flex:1,height:40,borderWidth:1,borderColor:theme.border,borderRadius:10,flexDirection:'row',alignItems:'center',gap:8,paddingLeft:11,paddingRight:5,backgroundColor:theme.cream},input:{flex:1,fontSize:11,color:theme.ink,paddingVertical:8},clear:{width:32,height:32,alignItems:'center',justifyContent:'center'}});
