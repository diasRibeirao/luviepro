import {constantTimeEqual} from './constant-time';import {validIdempotencyKey} from './idempotency';describe('security utils',()=>{it('compares secrets safely',()=>{expect(constantTimeEqual('abcdefgh','abcdefgh')).toBe(true);expect(constantTimeEqual('abcdefgh','abcdefgi')).toBe(false)});it('validates idempotency keys',()=>{expect(validIdempotencyKey('checkout:12345678')).toBe(true);expect(validIdempotencyKey('bad key')).toBe(false)});});
