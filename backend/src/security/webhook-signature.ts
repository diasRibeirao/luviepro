import { createHmac, timingSafeEqual } from 'crypto';
export function parseSignatureHeader(value?:string):Readonly<Record<string,string>>{if(!value)return {};return Object.fromEntries(value.split(',').map(x=>x.trim()).filter(Boolean).map(part=>part.split('=',2)).filter(([k,v])=>Boolean(k&&v))) as Record<string,string>}
export function verifyMercadoPagoSignature(dataId:string,requestId:string|undefined,signature:string|undefined,secret:string):boolean{if(!dataId||!requestId||!signature||!secret)return false;const parts=parseSignatureHeader(signature);if(!parts.ts||!parts.v1)return false;const manifest=`id:${dataId.toLowerCase()};request-id:${requestId};ts:${parts.ts};`;const expected=createHmac('sha256',secret).update(manifest).digest('hex');const received=parts.v1;return expected.length===received.length&&timingSafeEqual(Buffer.from(expected),Buffer.from(received));}
