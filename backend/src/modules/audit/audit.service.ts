import { ForbiddenException, Injectable } from '@nestjs/common'; import { PrismaService } from '../../prisma.service'; import { AuditQueryDto } from './dto/audit.dto';
@Injectable() export class AuditService {constructor(private db:PrismaService){} async list(tenantId:string,filters:AuditQueryDto={}){const tenant=await this.db.tenant.findUnique({where:{id:tenantId}});const limit=tenant?await this.db.planLimit.findUnique({where:{plan:tenant.plan}}):null;if(!limit?.auditAccess)throw new ForbiddenException('Histórico de atividades disponível no plano Business');const where:any={tenantId};if(filters.action)where.action=filters.action;if(filters.entity)where.entity=filters.entity;if(filters.actorUserId)where.actorUserId=filters.actorUserId;const from=filters.from?new Date(filters.from):null,to=filters.to?new Date(filters.to):null;if((from&&!Number.isNaN(from.getTime()))||(to&&!Number.isNaN(to.getTime()))){where.createdAt={};if(from&&!Number.isNaN(from.getTime()))where.createdAt.gte=from;if(to&&!Number.isNaN(to.getTime())){to.setHours(23,59,59,999);where.createdAt.lte=to;}}const take=Math.min(500,Math.max(1,Number(filters.limit)||200));const[logs,users]=await Promise.all([this.db.auditLog.findMany({where,orderBy:{createdAt:'desc'},take}),this.db.user.findMany({where:{tenantId},select:{id:true,name:true,email:true}})]);const actors=new Map(users.map(u=>[u.id,u]));let items=logs.map(log=>({...log,actor:log.actorUserId?actors.get(log.actorUserId)??null:null}));const search=String(filters.search??'').trim().toLowerCase();if(search)items=items.filter((log:any)=>[log.action,log.entity,log.entityId,log.actor?.name,log.actor?.email,JSON.stringify(log.metadata??{})].some(v=>String(v??'').toLowerCase().includes(search)));return {items,total:items.length,actors:users};}}
