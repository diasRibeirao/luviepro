import { Controller,Get,Query,Req } from '@nestjs/common'; import { Permissions } from '../../permissions.guard'; import { Roles } from '../../roles.guard'; import { TenantRequest } from '../../request-user'; import { AuditQueryDto } from './dto/audit.dto'; import { AuditService } from './audit.service'; @Controller('audit-logs') export class AuditController {constructor(private audit:AuditService){} @Roles('owner','admin') @Permissions('audit.read') @Get() list(@Req() r:TenantRequest,@Query() q:AuditQueryDto){return this.audit.list(r.user.tenantId,q)}}
