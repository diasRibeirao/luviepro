import { Controller,Get,Req } from '@nestjs/common'; import { Permissions } from '../../permissions.guard'; import { TenantRequest } from '../../request-user'; import { DashboardService } from './dashboard.service'; @Controller('dashboard') export class DashboardController {constructor(private dashboard:DashboardService){} @Permissions('dashboard.read') @Get() get(@Req() r:TenantRequest){return this.dashboard.overview(r.user.tenantId,r.user.sub)}}
