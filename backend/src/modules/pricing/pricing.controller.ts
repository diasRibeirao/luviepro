import { Body,Controller,Post } from '@nestjs/common'; import { Public } from '../../public.decorator'; import { CalculateDto } from '../quotes/dto/quotes.dto'; import { PricingService } from './pricing.service'; @Controller('pricing') export class PricingController {constructor(private pricing:PricingService){} @Public() @Post('calculate') calculate(@Body() b:CalculateDto){return this.pricing.calculate(b)}}
