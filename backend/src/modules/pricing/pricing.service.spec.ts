import { BadRequestException } from '@nestjs/common'; import { PricingService } from './pricing.service'; describe('PricingService',()=>{const s=new PricingService();it('calculates per-day pricing',()=>expect(s.calculate({dailyRateCents:50000,days:5,people:2,variableCostCents:5000,fixedCostCents:10000,safetyMarginBps:2000} as any)).toEqual({laborCents:250000,variableCents:25000,fixedCents:10000,marginCents:55000,totalCents:340000}));it('rejects negative numeric input',()=>expect(()=>s.calculate({dailyRateCents:-1,days:1,people:1,variableCostCents:0,fixedCostCents:0,safetyMarginBps:0} as any)).toThrow(BadRequestException));});
