import {BillingReconciliationWorker} from './billing-reconciliation.worker';describe('BillingReconciliationWorker',()=>{it('uses worker lock before reconciling',async()=>{const billing:any={reconcilePendingBatch:jest.fn().mockResolvedValue({scanned:1,approved:1,pending:0,failed:0,skipped:0})};const redis:any={withWorkerLock:jest.fn(async(_k:string,_t:number,cb:any)=>({acquired:true,value:await cb()}))};await (new BillingReconciliationWorker(billing,redis) as any).tick();expect(redis.withWorkerLock).toHaveBeenCalledWith('billing:reconciliation-worker',55000,expect.any(Function));expect(billing.reconcilePendingBatch).toHaveBeenCalledWith(25,5)});it('does not run without lock',async()=>{const billing:any={reconcilePendingBatch:jest.fn()};const redis:any={withWorkerLock:jest.fn().mockResolvedValue({acquired:false})};await (new BillingReconciliationWorker(billing,redis) as any).tick();expect(billing.reconcilePendingBatch).not.toHaveBeenCalled()})});
