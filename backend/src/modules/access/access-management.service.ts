import { BadRequestException, ConflictException, ForbiddenException, Injectable, NotFoundException } from '@nestjs/common';
import { hash } from 'bcryptjs';
import { Prisma } from '../../../../generated-prisma';
import { createHash, randomBytes } from 'crypto';
import { PrismaService } from '../../prisma.service';
import { MailService } from '../../mail.service';
import { AUTH_SECURITY } from '../auth/auth-security';
import { AuthSessionService } from '../auth/auth-session.service';
import { CreateAccessProfileDto, CreateUserDto, UpdateAccessProfileDto, UpdateUserDto } from './dto/access.dto';

@Injectable()
export class AccessManagementService {
  private readonly permissionCatalog=['dashboard.read','clients.read','clients.write','services.read','services.write','quotes.read','quotes.write','orders.read','orders.write','purchases.read','purchases.write','projects.read','projects.write','calendar.read','calendar.write','finance.read','finance.write','settings.manage','users.manage','audit.read'];
  constructor(private readonly db:PrismaService,private readonly mail:MailService,private readonly sessions:AuthSessionService){}

  private audit(tenantId:string,actorUserId:string|undefined,action:string,entity:string,entityId?:string,metadata?:any){
    return this.db.auditLog.create({data:{tenantId,actorUserId,action,entity,entityId,metadata}});
  }
  private invitationHash(token:string){return createHash('sha256').update(token).digest('hex');}
  private invitationUrl(token:string){const base=(process.env.APP_WEB_URL||'http://localhost:8081').replace(/\/$/,'');return `${base}/invite/${encodeURIComponent(token)}`;}
  private normalizePermissions(values:any){const set=new Set(Array.isArray(values)?values.map(String):[]);return this.permissionCatalog.filter(code=>set.has(code));}
  private roleLabel(role:string){return ({admin:'Administrador',commercial:'Comercial',operational:'Operacional',finance:'Financeiro'} as Record<string,string>)[role]??role;}
  private async resolveCustomProfile(tenantId:string,customProfileId?:string){if(!customProfileId)return null;const tenant=await this.db.tenant.findUnique({where:{id:tenantId}});const limit=tenant?await this.db.planLimit.findUnique({where:{plan:tenant.plan}}):null;if(!limit?.customRoles)throw new ForbiddenException('Perfis personalizados estão disponíveis no plano Business');const profile=await this.db.accessProfile.findFirst({where:{id:customProfileId,tenantId,active:true}});if(!profile)throw new BadRequestException('Perfil personalizado inválido ou inativo');return profile;}
  private async accessCapacity(tenantId:string){const tenant=await this.db.tenant.findUnique({where:{id:tenantId}});if(!tenant)throw new NotFoundException('Conta não encontrada');const limit=await this.db.planLimit.findUnique({where:{plan:tenant.plan}});const now=new Date();const [active,pending]=await Promise.all([this.db.user.count({where:{tenantId,active:true}}),this.db.userInvitation.count({where:{tenantId,status:'pending',expiresAt:{gt:now}}})]);return {tenant,limit,active,pending,used:active+pending};}

  users(tenantId:string){return this.db.user.findMany({where:{tenantId},select:{id:true,name:true,email:true,role:true,customProfileId:true,customProfile:{select:{id:true,name:true,active:true}},active:true,createdAt:true,updatedAt:true},orderBy:[{role:'asc'},{name:'asc'}]});}
  async accessProfiles(tenantId:string){const tenant=await this.db.tenant.findUnique({where:{id:tenantId}});const limit=tenant?await this.db.planLimit.findUnique({where:{plan:tenant.plan}}):null;if(!limit?.customRoles)throw new ForbiddenException('Perfis personalizados estão disponíveis no plano Business');return this.db.accessProfile.findMany({where:{tenantId},orderBy:[{active:'desc'},{name:'asc'}]});}
  async createAccessProfile(tenantId:string,data:CreateAccessProfileDto,actorUserId:string){const tenant=await this.db.tenant.findUnique({where:{id:tenantId}});const limit=tenant?await this.db.planLimit.findUnique({where:{plan:tenant.plan}}):null;if(!limit?.customRoles||!limit?.granularPermissions)throw new ForbiddenException('Perfis personalizados e permissões granulares estão disponíveis no plano Business');const name=String(data.name??'').trim();if(name.length<2)throw new BadRequestException('Informe o nome do perfil');const permissions=this.normalizePermissions(data.permissions);if(!permissions.length)throw new BadRequestException('Selecione pelo menos uma permissão');const exists=await this.db.accessProfile.findFirst({where:{tenantId,name:{equals:name,mode:'insensitive'}}});if(exists)throw new ConflictException('Já existe um perfil com este nome');const created=await this.db.accessProfile.create({data:{tenantId,name,description:data.description?.trim()||null,permissions}});await this.audit(tenantId,actorUserId,'create','access_profile',created.id,{name,permissions});return created;}
  async updateAccessProfile(tenantId:string,id:string,data:UpdateAccessProfileDto,actorUserId:string){const profile=await this.db.accessProfile.findFirst({where:{id,tenantId}});if(!profile)throw new NotFoundException('Perfil não encontrado');const patch:any={};if(data.name!==undefined)patch.name=String(data.name).trim();if(data.description!==undefined)patch.description=data.description?.trim()||null;if(data.permissions!==undefined){const permissions=this.normalizePermissions(data.permissions);if(!permissions.length)throw new BadRequestException('Selecione pelo menos uma permissão');patch.permissions=permissions;}if(data.active!==undefined)patch.active=!!data.active;if(data.active===false){const assigned=await this.db.user.count({where:{tenantId,customProfileId:id,active:true}});if(assigned>0)throw new BadRequestException('Desative ou altere os usuários vinculados antes de inativar este perfil');}const updated=await this.db.accessProfile.update({where:{id},data:patch});await this.audit(tenantId,actorUserId,'update','access_profile',id,patch);return updated;}

  async userInvitations(tenantId:string){const now=new Date();await this.db.userInvitation.updateMany({where:{tenantId,status:'pending',expiresAt:{lte:now}},data:{status:'expired'}});return this.db.userInvitation.findMany({where:{tenantId,status:{in:['pending','expired']}},select:{id:true,name:true,email:true,role:true,customProfileId:true,customProfile:{select:{id:true,name:true}},status:true,expiresAt:true,createdAt:true,updatedAt:true},orderBy:{createdAt:'desc'}});}
  async createUser(tenantId:string,data:CreateUserDto,actorUserId:string){const {tenant,limit,used}=await this.accessCapacity(tenantId);if(tenant.plan==='starter')throw new BadRequestException('O plano Starter permite somente o proprietário. Faça upgrade para adicionar usuários');if(limit&&limit.maxUsers>=0&&used>=limit.maxUsers)throw new BadRequestException('Limite de usuários do plano atingido. Convites pendentes também reservam vagas');const customProfile=await this.resolveCustomProfile(tenantId,data.customProfileId);if(!customProfile&&!limit?.standardRoles&&data.role!=='admin')throw new ForbiddenException('Perfis de acesso estão disponíveis nos planos Pro e Business');const email=String(data.email).trim().toLowerCase();if(await this.db.user.findUnique({where:{email}}))throw new ConflictException('Este e-mail já possui acesso ao LuviePro');const current=await this.db.userInvitation.findFirst({where:{tenantId,email,status:'pending',expiresAt:{gt:new Date()}}});if(current)throw new ConflictException('Já existe um convite pendente para este e-mail');const token=randomBytes(32).toString('hex'),expiresAt=new Date(Date.now()+Number(process.env.INVITATION_TTL_HOURS||48)*3600000);const invitation=await this.db.userInvitation.create({data:{tenantId,name:String(data.name).trim(),email,role:customProfile?'admin':data.role,customProfileId:customProfile?.id??null,tokenHash:this.invitationHash(token),invitedByUserId:actorUserId,expiresAt},select:{id:true,name:true,email:true,role:true,customProfileId:true,customProfile:{select:{id:true,name:true}},status:true,expiresAt:true,createdAt:true}});const inviteUrl=this.invitationUrl(token);let delivery:any={sent:false,reason:'not_configured'};try{delivery=await this.mail.sendUserInvitation({to:email,name:invitation.name,tenantName:tenant.name,roleLabel:invitation.customProfile?.name??this.roleLabel(invitation.role),inviteUrl,expiresAt});}catch{delivery={sent:false,reason:'send_failed'};}await this.audit(tenantId,actorUserId,'invite','user_invitation',invitation.id,{role:invitation.role,email,delivery:delivery.sent?'sent':delivery.reason});return {...invitation,delivery,inviteUrl};}
  async resendUserInvitation(tenantId:string,id:string,actorUserId:string){const invitation=await this.db.userInvitation.findFirst({where:{id,tenantId},include:{tenant:true,customProfile:true}});if(!invitation)throw new NotFoundException('Convite não encontrado');if(invitation.status==='accepted'||invitation.status==='cancelled')throw new BadRequestException('Este convite não pode ser reenviado');if(invitation.status!=='pending'||invitation.expiresAt.getTime()<=Date.now()){const {limit,used}=await this.accessCapacity(tenantId);if(limit&&limit.maxUsers>=0&&used>=limit.maxUsers)throw new BadRequestException('Limite de usuários do plano atingido');}if(await this.db.user.findUnique({where:{email:invitation.email}}))throw new ConflictException('Este e-mail já possui acesso ao LuviePro');const token=randomBytes(32).toString('hex'),expiresAt=new Date(Date.now()+Number(process.env.INVITATION_TTL_HOURS||48)*3600000);const updated=await this.db.userInvitation.update({where:{id},data:{tokenHash:this.invitationHash(token),status:'pending',expiresAt,invitedByUserId:actorUserId}});const inviteUrl=this.invitationUrl(token);let delivery:any={sent:false,reason:'not_configured'};try{delivery=await this.mail.sendUserInvitation({to:updated.email,name:updated.name,tenantName:invitation.tenant.name,roleLabel:invitation.customProfile?.name??this.roleLabel(updated.role),inviteUrl,expiresAt});}catch{delivery={sent:false,reason:'send_failed'};}await this.audit(tenantId,actorUserId,'resend_invite','user_invitation',id,{email:updated.email,delivery:delivery.sent?'sent':delivery.reason});return {id:updated.id,status:updated.status,expiresAt:updated.expiresAt,delivery,inviteUrl};}
  async cancelUserInvitation(tenantId:string,id:string,actorUserId:string){const invitation=await this.db.userInvitation.findFirst({where:{id,tenantId}});if(!invitation)throw new NotFoundException('Convite não encontrado');if(invitation.status!=='pending'&&invitation.status!=='expired')throw new BadRequestException('Este convite não pode ser cancelado');const updated=await this.db.userInvitation.update({where:{id},data:{status:'cancelled'}});await this.audit(tenantId,actorUserId,'cancel_invite','user_invitation',id,{email:updated.email});return {ok:true};}
  async invitationInfo(token:string){const invitation=await this.db.userInvitation.findUnique({where:{tokenHash:this.invitationHash(token)},include:{tenant:true,customProfile:true}});if(!invitation||invitation.status!=='pending')throw new NotFoundException('Convite inválido ou indisponível');if(invitation.customProfileId&&!invitation.customProfile?.active)throw new BadRequestException('O perfil associado a este convite está inativo. Solicite um novo convite');if(invitation.tenant.status!=='active'||(invitation.tenant.subscriptionExpiresAt&&invitation.tenant.subscriptionExpiresAt.getTime()<Date.now()))throw new ForbiddenException('A conta da empresa está indisponível ou com assinatura expirada');if(invitation.expiresAt.getTime()<=Date.now()){await this.db.userInvitation.update({where:{id:invitation.id},data:{status:'expired'}});throw new BadRequestException('Este convite expirou. Solicite um novo convite ao administrador');}return {name:invitation.name,email:invitation.email,role:invitation.role,roleLabel:invitation.customProfile?.name??this.roleLabel(invitation.role),customProfileId:invitation.customProfileId,tenantName:invitation.tenant.name,expiresAt:invitation.expiresAt};}
  async acceptInvitation(token:string,password:string){if(password.length<AUTH_SECURITY.minPasswordLength)throw new BadRequestException(`A senha deve ter pelo menos ${AUTH_SECURITY.minPasswordLength} caracteres`);const tokenHash=this.invitationHash(token);const passwordHash=await hash(password,AUTH_SECURITY.bcryptRounds);let result:any=null;for(let attempt=0;attempt<3&&!result;attempt++){try{result=await this.db.$transaction(async tx=>{const invitation=await tx.userInvitation.findUnique({where:{tokenHash},include:{tenant:true,customProfile:true}});if(!invitation||invitation.status!=='pending')throw new NotFoundException('Convite inválido ou indisponível');if(invitation.customProfileId&&!invitation.customProfile?.active)throw new BadRequestException('O perfil associado a este convite está inativo. Solicite um novo convite');if(invitation.tenant.status!=='active'||(invitation.tenant.subscriptionExpiresAt&&invitation.tenant.subscriptionExpiresAt.getTime()<Date.now()))throw new ForbiddenException('A conta da empresa está indisponível ou com assinatura expirada');if(invitation.expiresAt.getTime()<=Date.now()){await tx.userInvitation.updateMany({where:{id:invitation.id,status:'pending'},data:{status:'expired'}});throw new BadRequestException('Este convite expirou. Solicite um novo convite');}const claimed=await tx.userInvitation.updateMany({where:{id:invitation.id,status:'pending',expiresAt:{gt:new Date()}},data:{status:'accepted',acceptedAt:new Date()}});if(claimed.count!==1)throw new ConflictException('Este convite já foi utilizado ou alterado por outra operação');if(await tx.user.findUnique({where:{email:invitation.email}}))throw new ConflictException('Este e-mail já possui acesso ao LuviePro');const limit=await tx.planLimit.findUnique({where:{plan:invitation.tenant.plan}});const active=await tx.user.count({where:{tenantId:invitation.tenantId,active:true}});if(limit&&limit.maxUsers>=0&&active>=limit.maxUsers)throw new BadRequestException('A empresa atingiu o limite de usuários do plano. Fale com o administrador');const user=await tx.user.create({data:{tenantId:invitation.tenantId,name:invitation.name,email:invitation.email,passwordHash,role:invitation.role,customProfileId:invitation.customProfileId,active:true}});return {user,invitation};},{isolationLevel:Prisma.TransactionIsolationLevel.Serializable});}catch(error){const retryable=error instanceof Prisma.PrismaClientKnownRequestError&&error.code==='P2034';if(!retryable||attempt===2)throw error;}}if(!result)throw new ConflictException('O convite foi alterado por outra operação. Tente novamente.');const {user,invitation}=result;await this.audit(invitation.tenantId,user.id,'accept_invite','user_invitation',invitation.id,{role:user.role,email:user.email});const loginUrl=(process.env.APP_WEB_URL||'http://localhost:8081').replace(/\/$/,'');await this.mail.sendAccessActivated({to:user.email,name:user.name,tenantName:invitation.tenant.name,loginUrl}).catch(()=>undefined);return {ok:true,email:user.email,name:user.name,tenantName:invitation.tenant.name,message:'Acesso ativado com sucesso. Entre com seu e-mail e a senha que você acabou de definir.'};}
  async passwordResetTarget(tenantId:string,id:string,actorUserId:string){const user=await this.db.user.findFirst({where:{id,tenantId},select:{id:true,email:true,active:true}});if(!user)throw new NotFoundException('Usuário não encontrado');if(!user.active)throw new BadRequestException('Ative o usuário antes de enviar a recuperação de senha');await this.audit(tenantId,actorUserId,'password_reset_requested_by_owner','user',id,{email:user.email});return user;}
  async updateUser(tenantId:string,id:string,data:UpdateUserDto,actorUserId:string){const user=await this.db.user.findFirst({where:{id,tenantId}});if(!user)throw new NotFoundException('Usuário não encontrado');if(user.role==='owner'&&id!==actorUserId)throw new ForbiddenException('O usuário proprietário não pode ser alterado');if(id===actorUserId&&(data.active===false||data.role!==undefined||data.customProfileId))throw new BadRequestException('Você não pode remover seu próprio acesso de proprietário');if(data.active===true&&!user.active){const tenant=await this.db.tenant.findUnique({where:{id:tenantId}});const limit=tenant?await this.db.planLimit.findUnique({where:{plan:tenant.plan}}):null;const activeUsers=await this.db.user.count({where:{tenantId,active:true}});if(limit&&limit.maxUsers>=0&&activeUsers>=limit.maxUsers)throw new BadRequestException('Limite de usuários do plano atingido');}const customProfile=data.customProfileId===undefined?undefined:await this.resolveCustomProfile(tenantId,data.customProfileId||undefined);const updated=await this.db.$transaction(async tx=>{const result=await tx.user.update({where:{id},data:{name:data.name??user.name,role:user.role==='owner'?'owner':(customProfile?'admin':(data.role??user.role)),...(customProfile!==undefined?{customProfileId:customProfile?.id??null}:{}),active:data.active??user.active},select:{id:true,name:true,email:true,role:true,customProfileId:true,customProfile:{select:{id:true,name:true}},active:true,createdAt:true,updatedAt:true}});if(data.active===false)await tx.authSession.updateMany({where:{userId:id,tenantId,revokedAt:null},data:{revokedAt:new Date(),revokedReason:'user_deactivated'}});return result;});await this.audit(tenantId,actorUserId,'update','user',id,{role:updated.role,customProfileId:updated.customProfileId,active:updated.active});return updated;}
}
