import { ApiService } from './api.service';
describe('pricing',()=>{it('aplica margem sobre a diária total, conforme a planilha',()=>{const s=new ApiService({} as any,{} as any);expect(s.calculate({dailyRateCents:50000,days:5,people:2,variableCostCents:5000,fixedCostCents:10000,safetyMarginBps:2000})).toEqual({laborCents:250000,variableCents:50000,fixedCents:10000,marginCents:10000,totalCents:320000});});});
