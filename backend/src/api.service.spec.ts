import { ApiService } from './api.service';
describe('pricing',()=>{it('calcula custos variáveis por dia, conforme a planilha',()=>{const s=new ApiService({} as any,{} as any,{sendUserInvitation:jest.fn()} as any);expect(s.calculate({dailyRateCents:50000,days:5,people:2,variableCostCents:5000,fixedCostCents:10000,safetyMarginBps:2000})).toEqual({laborCents:250000,variableCents:25000,fixedCents:10000,marginCents:15000,totalCents:300000});});});
