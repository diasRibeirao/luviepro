import {readFileSync} from 'fs';import {resolve} from 'path';const read=(p:string)=>readFileSync(resolve(process.cwd(),'src',p),'utf8');describe('observability architecture',()=>{it('registers metrics and request instrumentation',()=>{expect(read('app.module.ts')).toContain('ObservabilityModule');expect(read('main.ts')).toContain('metricsMiddleware');});it('redacts secrets in structured logger',()=>expect(read('observability/logger.service.ts')).toContain('redact(meta)'));});
