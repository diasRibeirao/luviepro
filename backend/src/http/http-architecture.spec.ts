import {readFileSync} from 'fs';import {resolve} from 'path';const read=(f:string)=>readFileSync(resolve(process.cwd(),'src',f),'utf8');describe('HTTP infrastructure architecture',()=>{it('keeps bootstrap thin',()=>{const s=read('main.ts');expect(s).toContain('requestContextMiddleware');expect(s).toContain('rateLimitMiddleware');expect(s).not.toContain("path==='/api/auth/login'")});it('uses strict production worker locks',()=>{const r=read('redis.service.ts');expect(r).toContain('withDistributedLock');expect(r).toContain('withWorkerLock');for(const f of ['modules/auth/auth-session-cleanup.worker.ts','modules/billing/billing-reconciliation.worker.ts'])expect(read(f)).toContain('withWorkerLock')});it('removes controller anys for webhook and logo',()=>{expect(read('modules/billing/billing.controller.ts')).not.toContain('@Body() b:any');expect(read('modules/account/account.controller.ts')).not.toContain('@UploadedFile() f:any')})});
