export type CircuitState='closed'|'open'|'half_open';
export class CircuitBreaker{private failures=0;private openedAt=0;private probe=false;constructor(private readonly threshold=5,private readonly resetMs=30000,private readonly now:()=>number=()=>Date.now()){if(threshold<1||resetMs<1)throw new Error('CircuitBreaker configuration invalid');}state():CircuitState{if(!this.openedAt)return 'closed';if(this.now()-this.openedAt>=this.resetMs)return 'half_open';return 'open';}canRequest(){const state=this.state();if(state==='closed')return true;if(state==='open')return false;if(this.probe)return false;this.probe=true;return true;}success(){this.failures=0;this.openedAt=0;this.probe=false;}failure(){this.probe=false;this.failures++;if(this.failures>=this.threshold)this.openedAt=this.now();}snapshot(){return {state:this.state(),failures:this.failures};}}
