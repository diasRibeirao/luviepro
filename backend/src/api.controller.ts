/**
 * @deprecated Round 29: HTTP routes are owned by feature controllers.
 * Kept as a source-level migration marker so overlay deployments replace the former monolith.
 */
export const API_CONTROLLER_MIGRATED_TO_FEATURES = true;
